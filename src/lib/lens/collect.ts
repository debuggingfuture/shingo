export const collect = async (wallet:ethers.Wallet)=>{
    console.log('aaa')

    const lensClient = await createClient(wallet);

    console.log('createCollect', publicationId, profileId, metadata)
  

    
